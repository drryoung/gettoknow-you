/**
 * Central site URL for metadata, canonical links, and Open Graph.
 * Production host is www; apex redirects at the deployment layer.
 */
export const SITE_URL = "https://www.gettoknow.you";

/** Minimum published public works before a collection is browsable. */
export const PUBLIC_COLLECTION_MIN_WORKS = 2;
